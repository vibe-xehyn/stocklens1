:root {
  --bg: #F9FAFB;
  --white: #FFFFFF;
  --gray-50: #F2F4F6;
  --gray-100: #E5E7EB;
  --gray-200: #D1D5DB;
  --gray-300: #B0B8C1;
  --gray-400: #8B95A1;
  --gray-500: #6B7684;
  --gray-700: #4E5968;
  --gray-800: #333D4B;
  --gray-900: #191F28;
  --accent: #3182F6;
  --accent-light: #E8F3FF;
  --up-color: #F04452;
  --up-bg: #FFF0F0;
  --down-color: #3182F6;
  --down-bg: #F0F4FF;
  --green: #0ECB81;
  --radius-sm: 8px;
  --radius: 14px;
  --radius-lg: 20px;
  --shadow-sm: 0 1px 3px rgba(0,0,0,0.04);
  --shadow: 0 2px 8px rgba(0,0,0,0.06);
  --shadow-lg: 0 4px 16px rgba(0,0,0,0.08);
}

* { box-sizing: border-box; margin: 0; padding: 0; }
html { overflow-x: hidden; background: var(--bg); scroll-behavior: smooth; }
body {
  background: var(--bg); color: var(--gray-900);
  font-family: 'Noto Sans KR', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  min-height: 100vh; line-height: 1.5; -webkit-font-smoothing: antialiased; padding-bottom: 20px;
}
::-webkit-scrollbar { width: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: var(--gray-200); border-radius: 99px; }

/* ── Onboarding ──────────────────────────────────────────────────── */
.onboard-overlay {
  position: fixed; inset: 0; z-index: 1000; display: flex; align-items: center; justify-content: center;
  background: rgba(0,0,0,0.4); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); padding: 16px;
}
.onboard-card {
  background: var(--white); border-radius: var(--radius-lg); padding: 32px 24px;
  max-width: 420px; width: 100%; box-shadow: var(--shadow-lg); text-align: center;
  animation: slideUp 0.35s cubic-bezier(0.16, 1, 0.3, 1);
}
@keyframes slideUp { from { opacity:0; transform: translateY(16px); } to { opacity:1; transform: translateY(0); } }
.onboard-title { font-size: 20px; font-weight: 800; color: var(--gray-900); margin-bottom: 4px; }
.onboard-desc { font-size: 13px; color: var(--gray-500); margin-bottom: 24px; }
.onboard-section { margin-bottom: 20px; text-align: left; }
.onboard-label { font-size: 11px; font-weight: 700; color: var(--gray-500); text-transform: uppercase; letter-spacing: 0.8px; margin-bottom: 8px; }
.onboard-type-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
.onboard-type-btn {
  padding: 14px 12px; border-radius: var(--radius-sm); border: 1.5px solid var(--gray-200);
  background: var(--white); cursor: pointer; text-align: left; transition: all 0.2s; color: var(--gray-600);
}
.onboard-type-btn:hover { border-color: var(--accent); }
.onboard-type-btn.active { border-color: var(--accent); background: var(--accent-light); }
.onboard-type-btn .ot-title { font-size: 14px; font-weight: 700; color: var(--gray-900); margin-bottom: 2px; }
.onboard-type-btn .ot-desc { font-size: 11px; color: var(--gray-400); line-height: 1.5; }
.onboard-capital-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
.onboard-capital-btn {
  padding: 10px; border-radius: var(--radius-sm); border: 1.5px solid var(--gray-200);
  background: var(--white); color: var(--gray-600); font-size: 13px; font-weight: 600;
  cursor: pointer; transition: all 0.2s;
}
.onboard-capital-btn:hover { border-color: var(--accent); }
.onboard-capital-btn.active { border-color: var(--accent); background: var(--accent-light); color: var(--accent); font-weight: 700; }
.onboard-create-btn {
  width: 100%; padding: 14px; border-radius: var(--radius); border: none;
  background: var(--accent); color: #fff; font-size: 15px; font-weight: 700; cursor: pointer;
  transition: all 0.2s; margin-top: 4px; font-family: inherit;
}
.onboard-create-btn:active { transform: scale(0.98); }

/* ── Header ───────────────────────────────────────────────────────── */
.mock-header { background: var(--white); border-bottom: 1px solid var(--gray-100); position: sticky; top: 0; z-index: 100; }
.mock-header-inner { max-width: 640px; margin: 0 auto; display: flex; align-items: center; justify-content: space-between; height: 52px; padding: 0 20px; }
.mock-logo { font-size: 16px; font-weight: 800; color: var(--gray-900); cursor: pointer; letter-spacing: -0.3px; }
.header-spacer { flex: 1; }
.mock-exit-btn { font-size: 13px; font-weight: 600; color: var(--gray-500); text-decoration: none; padding: 6px 12px; border-radius: var(--radius-sm); transition: all 0.15s; }
.mock-exit-btn:hover { background: var(--gray-50); color: var(--gray-700); }

/* ── Account Tab Bar ──────────────────────────────────────────────── */
.account-tab-bar { display: flex; gap: 0; background: var(--white); border-bottom: 1px solid var(--gray-100); padding: 0 20px; position: sticky; top: 52px; z-index: 99; }
.acct-tab { flex: 1; padding: 14px 0 12px; border: none; border-bottom: 2px solid transparent; background: transparent; font-size: 14px; font-weight: 700; color: var(--gray-400); cursor: pointer; transition: all 0.2s; font-family: inherit; }
.acct-tab.active { color: var(--gray-900); border-bottom-color: var(--accent); }
.acct-tab:not(.active):hover { color: var(--gray-600); }

/* ── Account List Bar ─────────────────────────────────────────────── */
.account-list-bar { display: flex; gap: 6px; padding: 10px 20px; overflow-x: auto; background: var(--white); border-bottom: 1px solid var(--gray-100); -webkit-overflow-scrolling: touch; }
.account-list-bar::-webkit-scrollbar { display: none; }
.acct-chip { padding: 7px 14px; border-radius: 20px; border: 1px solid var(--gray-200); background: var(--white); font-size: 12.5px; font-weight: 600; color: var(--gray-600); cursor: pointer; white-space: nowrap; transition: all 0.15s; font-family: inherit; flex-shrink: 0; }
.acct-chip.active { background: var(--accent); color: #fff; border-color: var(--accent); }
.acct-chip:not(.active):hover { border-color: var(--accent); color: var(--accent); }

/* ── Main Content ─────────────────────────────────────────────────── */
.mock-content { max-width: 640px; margin: 0 auto; padding: 0 20px; }

/* ── Total Asset ──────────────────────────────────────────────────── */
.total-asset-section { text-align: center; padding: 32px 0 24px; }
.total-asset-label { font-size: 13px; color: var(--gray-500); font-weight: 500; margin-bottom: 4px; }
.total-asset-value { font-size: 34px; font-weight: 800; color: var(--gray-900); letter-spacing: -0.5px; }
.total-asset-change-wrap { margin-top: 6px; display: flex; align-items: center; justify-content: center; gap: 8px; }
.total-asset-change { font-size: 16px; font-weight: 700; }
.total-asset-sub { font-size: 13px; color: var(--gray-500); }
.up { color: var(--up-color); }
.down { color: var(--down-color); }

/* ── Chart ────────────────────────────────────────────────────────── */
.chart-section { background: var(--white); border: 1px solid var(--gray-100); border-radius: var(--radius-lg); padding: 20px; margin-bottom: 14px; box-shadow: var(--shadow-sm); }
.chart-tabs-row { display: flex; gap: 4px; margin-bottom: 16px; background: var(--gray-50); border-radius: var(--radius-sm); padding: 3px; }
.range-tab { flex: 1; padding: 7px 0; border-radius: 6px; border: none; background: transparent; font-size: 12px; font-weight: 600; color: var(--gray-500); cursor: pointer; transition: all 0.15s; font-family: inherit; }
.range-tab.active { background: var(--white); color: var(--gray-900); box-shadow: var(--shadow-sm); }
.range-tab:not(.active):hover { color: var(--gray-700); }
.chart-wrap { width: 100%; height: 200px; position: relative; }

/* ── Allocation ───────────────────────────────────────────────────── */
.allocation-section { background: var(--white); border: 1px solid var(--gray-100); border-radius: var(--radius-lg); padding: 20px; margin-bottom: 14px; box-shadow: var(--shadow-sm); }
.allocation-grid { display: flex; align-items: center; gap: 24px; }
.pie-chart-wrap { width: 100px; height: 100px; flex-shrink: 0; }
.allocation-legend { flex: 1; display: flex; flex-direction: column; gap: 10px; }
.legend-row { display: flex; align-items: center; gap: 8px; font-size: 13px; }
.legend-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
.legend-label { flex: 1; color: var(--gray-700); font-weight: 500; }
.legend-value { font-weight: 700; color: var(--gray-900); }
.legend-pct { color: var(--gray-400); font-size: 12px; margin-left: 4px; }

/* ── Section Title ────────────────────────────────────────────────── */
.section-title-row { display: flex; align-items: center; justify-content: space-between; margin-bottom: 14px; }
.section-title { font-size: 16px; font-weight: 700; color: var(--gray-900); }
.section-count { font-size: 13px; color: var(--gray-400); font-weight: 500; }

/* ── Balance ──────────────────────────────────────────────────────── */
.balance-section { background: var(--white); border: 1px solid var(--gray-100); border-radius: var(--radius-lg); padding: 4px 0; margin-bottom: 14px; box-shadow: var(--shadow-sm); }
.balance-row { display: flex; align-items: center; justify-content: space-between; padding: 14px 20px; cursor: pointer; transition: background 0.15s; }
.balance-row:hover { background: var(--gray-50); }
.balance-row + .balance-row { border-top: 1px solid var(--gray-100); }
.balance-label-area { display: flex; align-items: center; gap: 10px; }
.balance-dot { width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; }
.balance-dot.krw { background: var(--down-color); }
.balance-dot.usd { background: var(--up-color); }
.balance-currency { font-size: 14px; font-weight: 600; color: var(--gray-900); }
.balance-value-area { display: flex; align-items: center; gap: 8px; }
.balance-value { font-size: 15px; font-weight: 700; color: var(--gray-900); }
.balance-krw-sub { font-size: 12px; color: var(--gray-400); }
.balance-action { font-size: 12px; color: var(--accent); font-weight: 600; padding: 4px 10px; border-radius: 12px; background: var(--accent-light); }

/* ── Holdings ─────────────────────────────────────────────────────── */
.holdings-section { background: var(--white); border: 1px solid var(--gray-100); border-radius: var(--radius-lg); padding: 20px; margin-bottom: 14px; box-shadow: var(--shadow-sm); }
.holdings-list { display: flex; flex-direction: column; }
.holding-card { padding: 14px 0; border-bottom: 1px solid var(--gray-50); cursor: pointer; transition: background 0.12s; }
.holding-card:last-child { border-bottom: none; }
.holding-card:active { background: var(--gray-50); margin: 0 -20px; padding: 14px 20px; border-radius: var(--radius-sm); }
.holding-top { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 8px; }
.holding-name { font-size: 15px; font-weight: 700; color: var(--gray-900); }
.holding-ticker { font-size: 12px; color: var(--gray-400); font-weight: 500; }
.holding-eval { text-align: right; }
.holding-eval-price { font-size: 15px; font-weight: 700; color: var(--gray-900); }
.holding-eval-profit { font-size: 13px; font-weight: 600; }
.holding-eval-profit.up { color: var(--up-color); }
.holding-eval-profit.down { color: var(--down-color); }
.holding-meta { display: flex; justify-content: space-between; align-items: center; }
.holding-qty-info { font-size: 12px; color: var(--gray-400); }
.holding-weight-bar { flex: 1; height: 4px; border-radius: 2px; background: var(--gray-100); margin: 0 12px; overflow: hidden; }
.holding-weight-fill { height: 100%; border-radius: 2px; background: var(--accent); }

/* ── Dividend ─────────────────────────────────────────────────────── */
.dividend-section { background: var(--white); border: 1px solid var(--gray-100); border-radius: var(--radius-lg); padding: 20px; margin-bottom: 14px; box-shadow: var(--shadow-sm); }
.dividend-list { display: flex; flex-direction: column; }
.div-item { display: flex; align-items: center; padding: 12px 0; gap: 12px; border-bottom: 1px solid var(--gray-50); }
.div-item:last-child { border-bottom: none; }
.div-dot { width: 32px; height: 32px; border-radius: 50%; flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: 14px; font-weight: 700; background: rgba(14,203,129,0.1); color: var(--green); }
.div-info { flex: 1; }
.div-ticker { font-size: 14px; font-weight: 700; color: var(--gray-900); }
.div-date { font-size: 12px; color: var(--gray-400); margin-top: 2px; }
.div-end { text-align: right; flex-shrink: 0; }
.div-amount { font-size: 14px; font-weight: 700; color: var(--green); }
.div-qty { font-size: 11px; color: var(--gray-400); margin-top: 2px; }

/* ── History ──────────────────────────────────────────────────────── */
.history-section { background: var(--white); border: 1px solid var(--gray-100); border-radius: var(--radius-lg); padding: 20px; margin-bottom: 14px; box-shadow: var(--shadow-sm); }
.history-list { display: flex; flex-direction: column; }
.history-item { display: flex; align-items: center; padding: 12px 0; gap: 12px; border-bottom: 1px solid var(--gray-50); }
.history-item:last-child { border-bottom: none; }
.history-dot-icon { width: 32px; height: 32px; border-radius: 50%; flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: 14px; font-weight: 700; }
.history-dot-icon.buy { background: var(--up-bg); color: var(--up-color); }
.history-dot-icon.sell { background: var(--down-bg); color: var(--down-color); }
.history-info { flex: 1; }
.history-ticker-row { display: flex; align-items: center; gap: 6px; }
.history-ticker { font-size: 14px; font-weight: 700; color: var(--gray-900); }
.history-side-tag { font-size: 11px; padding: 2px 6px; border-radius: 4px; font-weight: 600; }
.history-side-tag.buy { background: var(--up-bg); color: var(--up-color); }
.history-side-tag.sell { background: var(--down-bg); color: var(--down-color); }
.history-detail { font-size: 12px; color: var(--gray-400); margin-top: 2px; }
.history-end { text-align: right; flex-shrink: 0; }
.history-amount { font-size: 14px; font-weight: 700; color: var(--gray-900); }
.history-time { font-size: 11px; color: var(--gray-400); margin-top: 2px; }

/* ── FAB ──────────────────────────────────────────────────────────── */
.fab-container { position: fixed; bottom: 24px; right: calc((100vw - 640px) / 2 + 20px); z-index: 300; display: flex; gap: 10px; }
@media (max-width: 680px) { .fab-container { right: 20px; } }
.fab-btn { width: 52px; height: 52px; border-radius: 26px; border: none; font-size: 13px; font-weight: 700; color: #fff; cursor: pointer; box-shadow: var(--shadow-lg); transition: all 0.2s; font-family: inherit; }
.fab-btn:active { transform: scale(0.95); }
.fab-btn.buy { background: var(--up-color); }
.fab-btn.sell { background: var(--down-color); }
.fab-btn.account { background: var(--gray-500); width: auto; padding: 0 14px; font-size: 12px; }

/* ── Empty ────────────────────────────────────────────────────────── */
.empty-state { padding: 24px 0; text-align: center; color: var(--gray-400); font-size: 13px; line-height: 1.8; }

/* ── Bottom Sheet ─────────────────────────────────────────────────── */
.sheet-overlay { display: none; position: fixed; inset: 0; z-index: 500; background: rgba(0,0,0,0.3); }
.sheet-overlay.open { display: block; }
.bottom-sheet { position: fixed; bottom: 0; left: 50%; transform: translateX(-50%) translateY(100%); width: 100%; max-width: 640px; max-height: 82vh; background: var(--white); border-radius: var(--radius-lg) var(--radius-lg) 0 0; z-index: 501; overflow-y: auto; box-shadow: 0 -4px 20px rgba(0,0,0,0.08); transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1); padding-bottom: env(safe-area-inset-bottom); }
.bottom-sheet.open { transform: translateX(-50%) translateY(0); }
.sheet-handle { width: 36px; height: 4px; background: var(--gray-200); border-radius: 2px; margin: 12px auto 8px; }
.sheet-header { display: flex; align-items: center; justify-content: space-between; padding: 4px 20px 12px; }
.sheet-header h3 { font-size: 18px; font-weight: 800; color: var(--gray-900); }
.sheet-close-btn { width: 32px; height: 32px; border-radius: 50%; border: none; background: var(--gray-50); cursor: pointer; display: flex; align-items: center; justify-content: center; transition: background 0.15s; }
.sheet-close-btn:hover { background: var(--gray-100); }
.sheet-body { padding: 0 20px 24px; }
.sheet-label { display: block; font-size: 12px; font-weight: 600; color: var(--gray-500); margin-bottom: 6px; }
.sheet-field { margin-bottom: 16px; }
.sheet-field input[type="text"] { width: 100%; padding: 12px 14px; border-radius: var(--radius-sm); border: 1px solid var(--gray-200); background: var(--gray-50); color: var(--gray-900); font-size: 15px; font-weight: 500; outline: none; font-family: inherit; transition: border-color 0.15s; }
.sheet-field input:focus { border-color: var(--accent); background: var(--white); }

.search-wrap { position: relative; }
.search-dropdown { display: none; position: absolute; top: 100%; left: 0; right: 0; max-height: 200px; overflow-y: auto; background: var(--white); border: 1px solid var(--gray-200); border-radius: var(--radius-sm); box-shadow: var(--shadow-lg); z-index: 10; margin-top: 4px; }
.search-dropdown.open { display: block; }
.search-dropdown-item { padding: 11px 14px; cursor: pointer; font-size: 13px; font-weight: 500; border-bottom: 1px solid var(--gray-50); transition: background 0.1s; display: flex; justify-content: space-between; align-items: center; }
.search-dropdown-item:hover { background: var(--gray-50); }
.search-dropdown-item:last-child { border-bottom: none; }
.sdi-ticker { font-weight: 700; color: var(--gray-900); }
.sdi-name { color: var(--gray-500); font-size: 12px; }
.sdi-market { font-size: 11px; color: var(--gray-400); }

.selected-stock-info { background: var(--gray-50); border-radius: var(--radius); padding: 14px 16px; margin-bottom: 14px; }
.ssi-row { display: flex; align-items: center; gap: 8px; margin-bottom: 6px; }
.ssi-name { font-size: 16px; font-weight: 700; color: var(--gray-900); }
.ssi-market-badge { font-size: 11px; color: var(--gray-500); font-weight: 600; background: var(--gray-100); padding: 2px 8px; border-radius: 4px; }
.ssi-price-row { display: flex; align-items: baseline; gap: 8px; }
.ssi-price { font-size: 22px; font-weight: 800; }
.ssi-price.up { color: var(--up-color); }
.ssi-price.down { color: var(--down-color); }
.ssi-change { font-size: 13px; font-weight: 600; }
.ssi-change.up { color: var(--up-color); }
.ssi-change.down { color: var(--down-color); }

.orderbook { margin-bottom: 14px; border-radius: var(--radius); overflow: hidden; border: 1px solid var(--gray-100); }
.ob-row { display: flex; align-items: center; padding: 7px 12px; font-size: 12.5px; font-family: 'SF Mono', 'Consolas', 'Noto Sans KR', monospace; gap: 8px; cursor: pointer; transition: background 0.1s; }
.ob-row:hover { background: var(--gray-50); }
.ob-row.ask { background: var(--up-bg); }
.ob-row.bid { background: var(--down-bg); }
.ob-row .ob-price { font-weight: 700; width: 70px; text-align: right; }
.ob-row.ask .ob-price { color: var(--up-color); }
.ob-row.bid .ob-price { color: var(--down-color); }
.ob-bar-wrap { flex: 1; height: 20px; position: relative; }
.ob-bar { position: absolute; top: 0; height: 100%; opacity: 0.15; border-radius: 2px; }
.ob-bar.ask { background: var(--up-color); right: 0; }
.ob-bar.bid { background: var(--down-color); left: 0; }
.ob-row .ob-vol { color: var(--gray-500); width: 50px; text-align: left; }
.ob-center { display: flex; justify-content: center; padding: 8px; font-weight: 700; font-size: 14px; background: var(--gray-50); color: var(--gray-900); }

.segmented-row { display: flex; gap: 2px; background: var(--gray-100); border-radius: var(--radius-sm); padding: 3px; }
.seg-btn { flex: 1; padding: 9px 0; border-radius: 6px; border: none; cursor: pointer; font-size: 13px; font-weight: 600; color: var(--gray-500); background: transparent; transition: all 0.15s; font-family: inherit; }
.seg-btn.active { background: var(--white); color: var(--gray-900); box-shadow: var(--shadow-sm); }
.seg-btn:not(.active):hover { color: var(--gray-700); }

.qty-row { display: flex; align-items: center; gap: 6px; }
.qty-row input { flex: 1; }
.qty-unit { font-size: 16px; font-weight: 600; color: var(--gray-700); }
.qty-presets { display: flex; gap: 6px; margin-top: 8px; }
.qty-presets button { flex: 1; padding: 8px 0; border-radius: var(--radius-sm); border: 1px solid var(--gray-200); background: var(--white); color: var(--gray-600); font-size: 12px; font-weight: 600; cursor: pointer; transition: all 0.15s; font-family: inherit; }
.qty-presets button:hover { border-color: var(--accent); color: var(--accent); }

.order-summary { background: var(--gray-50); border-radius: var(--radius); padding: 14px 16px; margin-bottom: 16px; }
.os-row { display: flex; justify-content: space-between; align-items: center; padding: 3px 0; font-size: 13px; color: var(--gray-600); }
.os-divider { height: 1px; background: var(--gray-200); margin: 8px 0; }
.os-row.os-total { font-weight: 700; color: var(--gray-900); font-size: 14px; }

.sheet-submit-btn { width: 100%; padding: 14px; border-radius: var(--radius); border: none; font-size: 16px; font-weight: 700; cursor: pointer; color: #fff; font-family: inherit; transition: all 0.2s; }
.sheet-submit-btn.buy { background: var(--up-color); }
.sheet-submit-btn.sell { background: var(--down-color); }
.sheet-submit-btn.neutral { background: var(--accent); }
.sheet-submit-btn:active { transform: scale(0.98); }
.sheet-submit-btn:disabled { opacity: 0.5; cursor: not-allowed; }

.fx-rate-card { text-align: center; padding: 16px; margin-bottom: 16px; background: var(--accent-light); border-radius: var(--radius); }
.fx-rate-label { font-size: 12px; color: var(--gray-500); font-weight: 500; }
.fx-rate-value { font-size: 18px; font-weight: 800; color: var(--accent); margin-top: 4px; }
.fx-preview { padding: 14px; margin-bottom: 16px; border-radius: var(--radius); background: var(--gray-50); text-align: center; font-size: 15px; font-weight: 600; color: var(--gray-800); }

/* ── Toast ────────────────────────────────────────────────────────── */
.toast-container { position: fixed; top: 16px; right: 16px; z-index: 9999; display: flex; flex-direction: column; gap: 8px; max-width: 320px; pointer-events: none; }
.toast { padding: 10px 16px; border-radius: var(--radius-sm); font-size: 13px; font-weight: 600; background: var(--gray-900); color: #fff; box-shadow: var(--shadow-lg); animation: toastIn 0.25s ease-out; }
@keyframes toastIn { from { opacity:0; transform: translateY(-8px); } to { opacity:1; transform: translateY(0); } }

/* ── Responsive ───────────────────────────────────────────────────── */
@media (max-width: 680px) {
  .mock-content { padding: 0 16px; }
  .account-tab-bar { padding: 0 16px; }
  .account-list-bar { padding: 10px 16px; }
  .total-asset-value { font-size: 30px; }
  .bottom-sheet { max-width: 100%; border-radius: var(--radius-lg) var(--radius-lg) 0 0; }
  .fab-btn { width: 48px; height: 48px; font-size: 12px; }
}